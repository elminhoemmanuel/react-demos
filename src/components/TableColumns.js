import React from 'react'

function TableColumns() {
    // const items =[]
    return (
        <React.Fragment>
            {/* {
                items.map(item=>(
                    <React.Fragment key={items.id}>
                        <p>{item.title}</p>
                    </React.Fragment>
                ))
            } */}
            <td>Emma</td>
            <td>Emma</td>
            
        </React.Fragment>
    )
}

export default TableColumns
